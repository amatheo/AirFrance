import { ClasseVolDirective } from './classevol.directive';

describe('ClasseVolDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasseVolDirective();
    expect(directive).toBeTruthy();
  });
});
