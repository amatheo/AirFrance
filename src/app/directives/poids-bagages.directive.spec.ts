import { PoidsBagagesDirective } from './poids-bagages.directive';

describe('PoidsBagagesDirective', () => {
  it('should create an instance', () => {
    const directive = new PoidsBagagesDirective();
    expect(directive).toBeTruthy();
  });
});
